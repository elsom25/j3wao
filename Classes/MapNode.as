package  
{	
	/*
	A map node is a node where the user can walk to. Each node contains the menu associated with it (ie. go to bar).
	*/
	public class MapNode 
	{
		public function MapNode() 
		{
			
		}
	}
}
