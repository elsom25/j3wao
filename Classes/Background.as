package  {
	
	import flash.display.BitmapData;
	public class Background extends BitmapData {
		
		// This symbol is used to change the backgrounds
		// There should be an instance of this symbol in the center position of each frame
		
		public function Background() {
			super(0,0);
		}
	}
	
}
